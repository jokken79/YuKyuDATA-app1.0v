# YuKyuDATA Configuration
DEBUG=true

# Database
DATABASE_URL=sqlite:///./yukyu.db
DATABASE_ENCRYPTION_KEY=generate-with-python-secrets-token-hex-32

# JWT & Auth
JWT_SECRET_KEY=generate-with-python-secrets-token-urlsafe-32
JWT_REFRESH_SECRET_KEY=generate-with-python-secrets-token-urlsafe-32
JWT_EXPIRATION_MINUTES=15

# Security
CORS_ORIGINS=http://localhost:8000,http://localhost:3000,http://127.0.0.1:8000
RATE_LIMIT_ENABLED=true
SSL_ENABLED=false

# Features
MFA_ENABLED=false

# Logging
LOG_LEVEL=INFO
